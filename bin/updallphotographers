#!/bin/bash

PHO_DIR="/mnt/transcend/Pictures/MagicMirror/Photographers"

[ -d ${PHO_DIR} ] || {
    echo "$PHO_DIR does not exist or is not a directory. Exiting."
    exit 1
}
cd ${PHO_DIR}

for folder in *
do
    updphotographer "$folder"
done
