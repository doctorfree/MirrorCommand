#!/bin/bash

ART_DIR="/mnt/transcend/Pictures/MagicMirror/Artists"

[ -d ${ART_DIR} ] || {
    echo "$ART_DIR does not exist or is not a directory. Exiting."
    exit 1
}
cd ${ART_DIR}

for folder in *
do
    updartist "$folder"
done
