#!/bin/bash

MOD_DIR="/mnt/transcend/Pictures/MagicMirror/Models"

[ -d ${MOD_DIR} ] || {
    echo "$MOD_DIR does not exist or is not a directory. Exiting."
    exit 1
}
cd ${MOD_DIR}

for folder in *
do
    updmodel "$folder"
done
